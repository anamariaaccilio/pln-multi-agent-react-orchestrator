# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Secrets / entorno
.env
*.key

# Modelos y cachés pesados
models/
.cache/
*.gguf
*.bin
*.safetensors

# Vector stores locales (módulo de recuperación)
*.faiss
chroma_db/
vectorstore/

# Salidas generadas del pipeline (mantener carpeta, ignorar contenido)
outputs/traces/*
!outputs/traces/.gitkeep
outputs/graph/*
!outputs/graph/.gitkeep

# outputs/evaluation_ready/ SI se versiona: son los resultados de la
# evaluacion (entregable), no basura transitoria. Solo se ignoran archivos
# de estado/debug de una corrida puntual, sin valor como entregable.
outputs/evaluation_ready/.key_rotation_state.json
outputs/evaluation_ready/*.log

# SO / editores
.DS_Store
.idea/
.vscode/
