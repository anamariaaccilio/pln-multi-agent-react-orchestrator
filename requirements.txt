langgraph>=0.2.28
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
pydantic>=2.7.0

# Vector store / embeddings (módulo de recuperación, incluido aqui para que el repo completo instale limpio)
faiss-cpu>=1.8.0
chromadb>=0.5.3
sentence-transformers>=3.0.1

# LLM local cuantizado (opcional, solo si USE_FALLBACK_LLM=False y provider='local')
torch>=2.3.0
transformers>=4.44.0
accelerate>=0.33.0
bitsandbytes>=0.43.1

# LLM via API (opcional, desarrollo rapido sin GPU)
google-generativeai>=0.7.2

# Evaluacion (módulo de evaluación)
ragas>=0.1.9
datasets>=2.20.0

# Utilidades
pyyaml>=6.0.1
python-dotenv>=1.0.1
pandas>=2.2.2
networkx>=3.3
matplotlib>=3.9.0
ipython
jupyter
